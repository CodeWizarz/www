features:
  secondary:
    - name: "GitLab Runner 13.10"
      available_in: [core, premium, ultimate]
      documentation_link: 'https://docs.gitlab.com/runner'
      reporter: deastman
      stage: verify
      categories:
        - 'GitLab Runner'
      description: |
        We’re also releasing GitLab Runner 13.10 today! GitLab Runner is the lightweight, highly-scalable agent that runs your build jobs and sends the results back to a GitLab instance. GitLab Runner works in conjunction with GitLab CI/CD, the open-source continuous integration service included with GitLab.

        #### What's new:

        - [Support graceful process termination on Windows, enabled by using the `FF_USE_WINDOWS_LEGACY_PROCESS_STRATEGY` feature flag](https://gitlab.com/gitlab-org/gitlab-runner/-/issues/4917).
        - [Update job log output limit exceeded warning](https://gitlab.com/gitlab-org/gitlab-runner/-/issues/27601).

