features:
  secondary:
    - name: "GitLab Runner 14.3"
      available_in: [core, premium, ultimate]
      documentation_link: 'https://docs.gitlab.com/runner'
      reporter: deastman
      stage: verify
      categories:
        - 'GitLab Runner'
      description: |
        We’re also releasing GitLab Runner 14.3 today! GitLab Runner is the lightweight, highly-scalable agent that runs your build jobs and sends the results back to a GitLab instance. GitLab Runner works in conjunction with GitLab CI/CD, the open-source continuous integration service included with GitLab.

        #### What's new:

        - [GitLab Runner packages for Fedora 33 & 34 now available](https://gitlab.com/gitlab-org/gitlab-runner/-/issues/27484)
        - [Assign runner nodes to Pods by using Kubernetes inter-pod affinity/anti-affinity](https://gitlab.com/gitlab-org/gitlab-runner/-/issues/3969)

        #### Bug Fixes:

        - [Shell executor doesn't clean up build directories](https://gitlab.com/gitlab-org/gitlab-runner/-/issues/3856)

        The list of all changes is in the GitLab Runner [CHANGELOG](https://gitlab.com/gitlab-org/gitlab-runner/blob/14-3-stable/CHANGELOG.md).
