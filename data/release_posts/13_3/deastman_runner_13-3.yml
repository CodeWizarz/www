features:
  secondary:
    - name: "GitLab Runner 13.3"
      available_in: [core, starter, premium, ultimate]
      documentation_link: 'https://docs.gitlab.com/runner'
      reporter: deastman
      stage: verify
      categories:
        - 'GitLab Runner'
      description: |
        We’re also releasing GitLab Runner 13.3 today! GitLab Runner is the lightweight, highly-scalable agent that runs your build jobs and sends the results back to a GitLab instance. GitLab Runner works in conjunction with GitLab CI/CD, the open-source continuous integration service included with GitLab.

        #### What's new:

        - [Add an ENTRYPOINT script to the GitLab Runner helper image](https://gitlab.com/gitlab-org/gitlab-runner/-/issues/3371)
        - [Update installed Git version for Windows helper images](https://gitlab.com/gitlab-org/gitlab-runner/-/merge_requests/2306)
        - [Update Ubuntu Docker container to Ubuntu 20.04](https://gitlab.com/gitlab-org/gitlab-runner/-/merge_requests/2286)

        #### Bug Fixes:

        - Fix: [Register with the `--run-untagged ` option not working with rpm/deb latest in gitlab-runner-13.2.0-1](https://gitlab.com/gitlab-org/gitlab-runner/-/issues/26609)
        - Fix: [SELinux alerts: SELinux is preventing gitlab-runner from name_connect access on the tcp_socket port 443](https://gitlab.com/gitlab-org/gitlab-runner/-/issues/3440)
        - Fix: [PowerShell error when running job](https://gitlab.com/gitlab-org/gitlab-runner/-/issues/26623)
        - Fix: [Network per build: health check for services time out](https://gitlab.com/gitlab-org/gitlab-runner/-/issues/25660)

        The list of all changes is in the GitLab Runner [CHANGELOG](https://gitlab.com/gitlab-org/gitlab-runner/blob/13-3-stable/CHANGELOG.md).
