features:
  secondary:
    - name: "GitLab Runner 15.7"
      available_in: [core, premium, ultimate]
      documentation_link: 'https://docs.gitlab.com/runner'
      reporter: deastman
      stage: verify
      categories:
        - 'GitLab Runner Core'
      description: |
        We’re also releasing GitLab Runner 15.7 today! GitLab Runner is the lightweight, highly-scalable agent that runs your CI/CD jobs and sends the results back to a GitLab instance. GitLab Runner works in conjunction with GitLab CI/CD, the open-source continuous integration service included with GitLab.

        #### What's new:
        
        - [Dynamically mask resolved vault variables](https://gitlab.com/gitlab-org/gitlab/-/issues/255186)
        - [Mark secret values fetched from HashiCorp Vault integration as `raw`](https://gitlab.com/gitlab-org/gitlab-runner/-/issues/27996)
        - [Configurable interval for disabling an unhealthy runner](https://gitlab.com/gitlab-org/gitlab-runner/-/issues/29218)
        
        #### Bug Fixes:

        - [Can't deploy runner with helm chart project when using the ubi-fips](https://gitlab.com/gitlab-org/charts/gitlab-runner/-/issues/410)
        
        The list of all changes is in the GitLab Runner [CHANGELOG](https://gitlab.com/gitlab-org/gitlab-runner/blob/15-7-stable/CHANGELOG.md).
