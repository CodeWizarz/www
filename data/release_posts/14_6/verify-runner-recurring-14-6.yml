features:
  secondary:
    - name: "GitLab Runner 14.6"
      available_in: [core, premium, ultimate]
      documentation_link: 'https://docs.gitlab.com/runner'
      reporter: deastman
      stage: verify
      categories:
        - 'GitLab Runner'
      description: |
        We’re also releasing GitLab Runner 14.6 today! GitLab Runner is the lightweight, highly-scalable agent that runs your build jobs and sends the results back to a GitLab instance. GitLab Runner works in conjunction with GitLab CI/CD, the open-source continuous integration service included with GitLab.

        #### What's new:

        - [Always include `idle` value in `gitlab_runner_jobs` Prometheus metric](https://gitlab.com/gitlab-org/gitlab-runner/-/issues/25845)

        #### Bug Fixes:

        - [PWSH Executor fails to terminate on job failure](https://gitlab.com/gitlab-org/gitlab-runner/-/issues/28440)
        - [Old Runner pods are never deleted](https://gitlab.com/gitlab-org/gitlab-runner/-/issues/27702)

        The list of all changes is in the GitLab Runner [CHANGELOG](https://gitlab.com/gitlab-org/gitlab-runner/blob/14-6-stable/CHANGELOG.md).
