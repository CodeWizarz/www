## Categories A-Z

| Category | Group |
| --- | --- |
<% data.categories.sort_by { |s, c| c.name }.each do |slug, category| %>
| <%= category.name %> (`<%= slug %>`) | <%= category_group_link(slug) %> |
<% end %>
