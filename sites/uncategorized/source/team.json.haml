- team_array = []
- team = Gitlab::Homepage.team.members.reject { |t| t.picture == '../gitlab-logo-extra-whitespace.png' }
- team.each do |member|
  - team_array << { picture: image_path("team/#{member.picture}") }
= team_array.to_json
